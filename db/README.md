# Database

Top-level database artifacts:
- `schema.sql` — full current schema (for reference / fresh setups)
- `seed.sql` — sample data for local development

Authoritative changes go through `backend/migrations/`. This folder is for convenience snapshots and seed data.
