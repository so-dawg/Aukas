# Routes

Express route definitions. Each file maps URL paths to controller functions.

Example: `opportunities.routes.js` defines `GET /opportunities`, `POST /opportunities`, etc., and delegates to `controllers/opportunities.controller.js`.
